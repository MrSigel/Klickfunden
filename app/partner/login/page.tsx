import type {Metadata} from "next";import {redirect} from "next/navigation";import Header from "@/components/Header";import Footer from "@/components/Footer";import PartnerLoginForm from "@/components/partner/PartnerLoginForm";import {getPartnerSession} from "@/lib/partner-auth";
export const metadata:Metadata={title:"Partner Login | Klickfunden",robots:{index:false,follow:false}};export default function Page(){if(getPartnerSession())redirect("/partner/dashboard");return <><Header/><main className="min-h-screen bg-ink px-5 pb-20 pt-40 text-white"><PartnerLoginForm/></main><Footer/></>}
