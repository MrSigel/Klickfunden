import type {Metadata} from "next";import IndustryLandingPage from "@/components/IndustryLandingPage";import {industryLandingPages} from "@/lib/seo/industry-landing-pages";const page=industryLandingPages.dienstleister;export const metadata:Metadata={title:page.metaTitle,description:page.metaDescription,alternates:{canonical:page.path},openGraph:{title:page.metaTitle,description:page.metaDescription,url:`https://www.klickfunden.de${page.path}`,type:"website"}};export default function Page(){return <IndustryLandingPage page={page}/>}
